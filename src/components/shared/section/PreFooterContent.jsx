import React from 'react';

const PreFooterContent = ({ children }) => {
    return (
        <div className="pre-footer-container">
            <div className="container">{children}</div>
        </div>
    );
};

export default PreFooterContent;
